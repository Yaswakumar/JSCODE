const baseValue = ('Enter the base of a triangle: ');
const heightValue = ('Enter the height of a triangle: ');

// calculate the area
const areaValue = (5 * 6) / 2;

console.log(
  `The area of the triangle is ${areaValue}`
);